# Configuration du client web SupChat
# Copiez ce fichier en .env et ajustez les valeurs selon votre environnement

# URL du serveur backend (pour le proxy Vite en développement)
VITE_BACKEND_URL=http://localhost:3000

# URL de l'API pour les appels côté client
VITE_API_URL=http://localhost:3000

# URL WebSocket pour les connexions temps réel
VITE_WEBSOCKET_URL=http://localhost:3000

# Configuration OAuth Google (côté client)
# Seul le client ID peut être exposé côté client, pas le secret
VITE_GOOGLE_CLIENT_ID=votre-google-client-id.apps.googleusercontent.com

# Configuration OAuth Facebook (côté client)  
# Seul l'app ID peut être exposé côté client, pas le secret
VITE_FACEBOOK_APP_ID=votre-facebook-app-id

# URLs de redirection OAuth (doivent pointer vers le backend)
VITE_GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
VITE_FACEBOOK_REDIRECT_URI=http://localhost:3000/api/auth/facebook/callback

# IMPORTANT: Les secrets JWT, mots de passe et secrets OAuth 
# NE DOIVENT PAS être dans le .env du client web car ils seraient exposés côté client.
# Ces secrets doivent être uniquement dans le .env du serveur backend.
